const style = ({ height }) => ({
  container: {
    height,
  },
  iconContainer: {
    width: "10rem",
    height: "10rem",
    borderRadius: "8rem",
    border: "1px dashed",
    display: "flex",
    justifyContent: "center",
    alignItems: "center",
    backgroundColor: "cornsilk",
    margin: "auto",
  },
});

export default style;
